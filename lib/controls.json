{
  "name": "controls",
  "description": "BPscript controls — engine [] and runtime () separated",
  "version": "0.2.0",

  "subgrammar": {
    "_comment": "@mode:X(modifier) syntax — emitted as BP3 preamble between mode line and rules",
    "destru": {
      "bp3": "_destru",
      "args": [],
      "description": "Déstructure les terminaux composés en fonction de l'alphabet"
    },
    "striated": {
      "bp3": "_striated",
      "args": [],
      "description": "Temps strié (pulsé)"
    },
    "smooth": {
      "bp3": "_smooth",
      "args": [],
      "description": "Temps lisse (non pulsé)"
    },
    "mm": {
      "bp3": "_mm",
      "args": ["bpm"],
      "description": "Marquage métronomique absolu (BPM)"
    }
  },

  "engine": {
    "_comment": "[] syntax — translated directly to BP3 instructions by the transpiler",
    "tempo": {
      "bp3": "_tempo",
      "args": ["ratio"],
      "description": "Multiplicateur de tempo relatif"
    },
    "repeat": {
      "bp3": "_repeat",
      "args": ["expr"],
      "description": "Répétition contrôlée. expr = K-paramètre ou K-paramètre=valeur."
    },
    "failed": {
      "bp3": "_failed",
      "args": ["subgrammar", "rule"],
      "description": "Saut en cas d'échec de dérivation."
    },
    "stop": {
      "bp3": "_stop",
      "args": [],
      "description": "Arrête la dérivation."
    },
    "goto": {
      "bp3": "_goto",
      "args": ["subgrammar", "rule"],
      "description": "Saut vers une sous-grammaire et règle spécifiques."
    },
    "retro": {
      "bp3": "_retro",
      "args": [],
      "description": "Rétrograde — inverse l'ordre des éléments dans le contexte polymétrique."
    },
    "staccato": {
      "bp3": "_staccato",
      "args": ["value"],
      "range": [0, 127],
      "description": "Staccato — raccourcit la durée des notes (affecte la structure temporelle)"
    },
    "legato": {
      "bp3": "_legato",
      "args": ["value"],
      "range": [0, 127],
      "description": "Legato — prolonge la durée des notes (affecte la structure temporelle)"
    }
  },

  "runtime": {
    "_comment": "() syntax — transpiler emits _script(CTn), dispatcher interprets at playback",
    "vel": {
      "args": ["value"],
      "range": [0, 127],
      "default": 64,
      "description": "Vélocité (0-127)"
    },
    "chan": {
      "args": ["channel"],
      "range": [1, 16],
      "default": 1,
      "description": "Canal MIDI"
    },
    "pan": {
      "args": ["value"],
      "range": [0, 127],
      "default": 64,
      "description": "Panoramique (0=gauche, 64=centre, 127=droite)"
    },
    "transpose": {
      "args": ["steps"],
      "default": 0,
      "description": "Transposition — grid shift de N steps dans le tempérament",
      "transport": "dispatcher"
    },
    "ins": {
      "args": ["program"],
      "range": [1, 128],
      "default": 0,
      "description": "Program change MIDI"
    },
    "mod": {
      "args": ["value"],
      "range": [0, 127],
      "default": 0,
      "description": "Modulation MIDI (CC1)"
    },
    "pitchbend": {
      "args": ["value"],
      "range": [-8192, 8191],
      "default": 0,
      "description": "Pitch bend MIDI"
    },
    "pitchrange": {
      "args": ["cents"],
      "default": 200,
      "description": "Plage de pitch bend en cents"
    },
    "pitchcont": {
      "args": [],
      "description": "Active l'interpolation continue de pitch bend"
    },
    "pitchfixed": {
      "args": [],
      "description": "Désactive le mode continu de pitch bend"
    },
    "pressure": {
      "args": ["value"],
      "range": [0, 127],
      "default": 0,
      "description": "Channel pressure MIDI"
    },
    "press": {
      "args": ["value"],
      "range": [0, 127],
      "default": 0,
      "description": "Channel pressure MIDI (alias)"
    },
    "presscont": {
      "args": [],
      "description": "Active l'interpolation continue de channel pressure"
    },
    "volume": {
      "args": ["value"],
      "range": [0, 127],
      "default": 100,
      "description": "Volume MIDI (CC7)"
    },
    "volumecont": {
      "args": [],
      "description": "Active l'interpolation continue de volume"
    },
    "velcont": {
      "args": [],
      "description": "Active l'interpolation continue de vélocité"
    },
    "rndvel": {
      "args": ["range"],
      "default": 0,
      "description": "Vélocité aléatoire ±range"
    },
    "modcont": {
      "args": [],
      "description": "Active l'interpolation continue de modulation (CC1)"
    },
    "value": {
      "args": ["param", "amount"],
      "description": "Valeur d'un paramètre continu de sound object"
    },
    "fixed": {
      "args": ["param", "amount"],
      "description": "Valeur fixe d'un paramètre de sound object"
    },
    "cont": {
      "args": ["param"],
      "description": "Active le mode continu pour un paramètre"
    },
    "script": {
      "args": ["command"],
      "description": "Commande script (MIDI program change, etc.)"
    },
    "switchon": {
      "args": ["channel"],
      "description": "Active un canal MIDI switch"
    },
    "switchoff": {
      "args": ["channel"],
      "description": "Désactive un canal MIDI switch"
    },
    "wave": {
      "args": ["type"],
      "values": ["sine", "triangle", "square", "sawtooth"],
      "default": "triangle",
      "description": "Forme d'onde oscillateur (Web Audio)",
      "transport": "webaudio"
    },
    "attack": {
      "args": ["ms"],
      "range": [1, 5000],
      "default": 20,
      "description": "Attaque enveloppe en ms (Web Audio)",
      "transport": "webaudio"
    },
    "release": {
      "args": ["ms"],
      "range": [1, 5000],
      "default": 100,
      "description": "Relâchement enveloppe en ms (Web Audio)",
      "transport": "webaudio"
    },
    "detune": {
      "args": ["cents"],
      "range": [-1200, 1200],
      "default": 0,
      "description": "Désaccordage en cents (Web Audio)",
      "transport": "webaudio"
    },
    "filter": {
      "args": ["freq"],
      "range": [20, 20000],
      "default": 0,
      "description": "Fréquence de coupure filtre passe-bas Hz (Web Audio)",
      "transport": "webaudio"
    },
    "filterQ": {
      "args": ["value"],
      "range": [0, 30],
      "default": 1,
      "description": "Résonance (Q) du filtre (Web Audio)",
      "transport": "webaudio"
    },
    "scale": {
      "args": ["name", "blockkey"],
      "default": "0,0",
      "description": "Échelle microtonale — nom + note de base. scale(0,0) = retour tempérament égal.",
      "transport": "dispatcher"
    },
    "rotate": {
      "args": ["degrees"],
      "default": 0,
      "description": "Rotation cyclique — décale de N degrés dans l'alphabet (diatonique). rotate(2) : C→E, D→F, etc.",
      "transport": "dispatcher"
    },
    "keyxpand": {
      "args": ["pivot", "factor"],
      "default": "0,1",
      "description": "Expansion/contraction d'intervalles autour d'un pivot. factor=2 double, factor=-1 inverse, factor=0.5 contracte.",
      "transport": "dispatcher"
    }
  }
}
