# BPScript — Backlog

Dernière mise à jour : 2026-04-06
Build : v3.3.19-wasm.8
S4 vs S5 : 16/29 EXACT, 13 DIFF, 7 SKIP, 2 MISSING

## Données — cohérence chaîne pitch (alphabet → fréquence)

- **BPS-1** `ouvert` — Tri des `bp3_*` dans temperaments.json (différé, 2026-06-17). `lib/temperaments.json` traîne ~150 entrées legacy `bp3_*` qui MÉLANGENT de vrais tempéraments (`bp3_werckmeister_3`, `bp3_meantone_*`, `bp3_kirnberger_*`…) avec des GAMMES déguisées en tempéraments (`bp3_Cmaj`, `bp3_todi1`, `bp3_asavari1`, `bp3_*_murcchana`…). À TRIER : les vrais tempéraments restent ; les « gammes » partent dans `scales.json`. Même nature de ménage que la consolidation maqams. Reporté (décision Romain) — à faire après la migration jins/maqams.

## Moteur BP3 — Investigation

- **32** `bloqué` — `#32` WriteMIDIbyte drift CC interpolés (NotReich). Root cause trouvée : CC volume interpolés dépassent le timestamp du NoteOn suivant, `OldMIDIfileTime` recule, delta perdu s'accumule. Deux fixes proposés (MIDIfiles.c ou MakeSound.c). **Status :** En attente décision Bernard (Fix A vs Fix B).
- **36** `fait` — `#36` Production TEXT sans séparateurs — RÉSOLU. Non reproductible sur wasm.8. `getResult()` retourne les terminaux séparés avec et sans alphabet.

### Résolus

- **33** `fait` — `#33` NoteOff retardé → dedup keep-longest WASM (wasm.2)
- **35** `fait` — `#35` Offset +10ms → Kpress offset WASM (wasm.3)
- **38** `fait` — `#38` T47 → Bernard implémenté v3.3.19, WASM adapté (wasm.4)
- **39** `fait` — `#39` ASLR p_DefaultChannel → GetRelease.c 3 points (wasm.5)
- **42** `fait` — `#42` Plot(ANYWHERE) écrase sentinelles -1 → FillPhaseDiagram.c (wasm.8)
- **43** `fait` — `#43` CT catchall `_script(CT N)` → ScriptUtils.c + console_strings.json (wasm.8)

## Transpileur S5

- **BPS-2** `abandonné` — COUNT diffs S4 vs S5 (11 grammaires). Cause à investiguer. Le pipeline S5 passe par le dispatcher (resolveTokens) qui applique transpose/rotate/etc. Les seuls contrôles qui causent un diff attendu sont ceux avec traitement audio webaudio (wave, filter, etc.) — non testables au niveau tokens.  _(abandonné: SANS OBJET — le pipeline S5 (et le dispatcher qu il traversait) ont ete SUPPRIMES le 2026-07-19 avec la voie 2 heritee. Il n y a plus de S4 ni de S5 a comparer.)_

Bugs restants. Détail dans `memory/backlog_s5_transpiler.md`.

- **BPS-3** `abandonné` — `_goto` : position dans la grammaire incorrect  _(abandonné: SANS OBJET — le defaut portait sur la POSITION d emission dans la grammaire BP3, et il n y a plus de grammaire emise. Verifie le 2026-07-19 : `[goto:2]` est bien porte dans l AST en Qualifier/QualPair.)_
- **BPS-4** `fait` — `mode` : gram#1 toujours LIN, devrait respecter @mode  _(fait: verifie le 2026-07-19 sur la voie AST — `@mode:ord` rend `subgrammars[0].mode === 'ord'`. Le mode est respecte ; le defaut etait dans l emission BP3, supprimee.)_
- **BPS-5** `fait` — Terminaux numériques purs (ex: `1`, `2`) confondus avec des vitesses  _(fait: verifie le 2026-07-19 — `S -> 1 2` rend deux noeuds `NumericTerminal`, pas des vitesses. La confusion vivait dans l encodeur, supprime.)_
- **BPS-6** `ouvert` [A REQUALIFIER] — Settings non fidèlement portés depuis les -se. de Bernard  _(2026-07-19 : le defaut d origine portait sur le fichier de reglages EMIS par l encodeur, supprime — donc SANS OBJET sous cette forme. MAIS la question de fond survit : les reglages du -se natif (horloge, plafond d items, C4key) doivent-ils entrer dans l AST ? Aujourd hui `ast.settings` est ABSENT. Ce n est plus un bug d emission, c est une question de contrat AST — a poser a bpx, pas a fermer en silence.)_

## Dispatcher / Runtime

- **BPS-7** `abandonné` — Resolver musical complet. Mapping note → fréquence en dur (Western 12-TET, A=440). Degrés : sargam → fréquences ; Tempérament : pythagoricien, meantone, just intonation ; Référence configurable : `@tuning:442` ; Microtonalité : échelles à N divisions.  _(abandonné: HORS PERIMETRE — la resolution de hauteur a MIGRE chez Kairos (src/pitch/, KAI-10 ; confirme par Kairos lui-meme le 2026-07-19 : memes noms de fonctions que l ancien dispatcher). Il n y a plus aucun mapping note vers frequence dans le transpileur — verifie, les seules occurrences de 440 sont un exemple dans un message d erreur et un defaut de reglages BP3. La microtonalite est verrouillee chez lui (microtonal-precision.test.ts, et le comma pythagoricien que je lui ai transmis).)_
- **BPS-8** `ouvert` [A REQUALIFIER] — Routage CV multi-cibles. CV crée un bus audio global. Besoin de routage par cible : `env1(Phrase1, browser)`.  _(2026-07-19 : la FORME citee est perimee — `browser` est un canal SUPPRIME (decision 2026-07-16, ecrire `audio`), et le bus audio global vivait dans le dispatcher, retire a l extraction Kanopi. Le BESOIN, lui, n est pas verifie : router un CV vers une cible nommee plutot que vers un bus unique. A reposer sur le modele actuel (runtimes de sortie) avant d etre chiffre — je ne la ferme pas sur la peremption de son exemple.)_
- **BPS-dhati-T0** `abandonné` — dhati operateurs T0 (coord BPx) : dhati.bps declare +/;/* en @gate X:midi (sonnants) au lieu d operateurs T0 BP3 — investiguer fixture vs besoin reel de mapping operateur->T0. Spec precise a venir de BPx. Escalader a Romain si vrai gap langage  _(abandonné: ferme : dhati-A est un fix LOCAL BPx (compileRhsElement traite +;* comme terminal sonnant au lieu de T0), PAS un manque langage (le transpileur mappe deja +;*). Aucun item BPScript)_
- **BPS-enc-tempo** `ouvert` — Fidelite encodeur : serialiser le [/N] COLLE (suffixe-element) pour qu il RE-PARSE en _tempo (round-trip fidele), PAS en prefixe nu /N {…} (rate). Oracle natif = verite ; distinction ESPACE=rate niveau-regle / COLLE=_tempo suffixe-element. NON bloquant (parse coherent end-to-end via m4_07)
- **BPS-defaut-env** `fait` — Point 1 (archi) : integrer les defauts d ENVIRONNEMENT dans l AST a la creation — transpiler(source, environnement) ; l environnement est porté par Kanopi, fourni en entree de transpilation ; le tempo par defaut (et octave/division…) entre dans l AST quand la scene ne declare rien. Kanopi ne touche JAMAIS l AST. Cf hub/projets/spec-ecriture-structure.md  _(fait: M5 : compileToBPxAST(source, environnement), defaut tempo dans l AST, preuve non-circulaire 13/13, ISO BP3 preservee — ac9a474)_
- **BPS-propagation** `fait` — Propagation DURABLE du param environnement (ac9a474) a l artefact CONSOMME par Kanopi (node_modules/bpscript via file:) : src OK mais le dist/copie consommee l ignore -> Kanopi a du rsync a la main, sur install frais M5 devient INERT SILENCIEUX. A faire : rebuild/republier proprement le file: dep + un TEST qui prouve l injection env->AST A TRAVERS le paquet consomme. Coord Kanopi  _(fait: package.json version+main (aff985d) + test-garde Kanopi (51e8cd7) ; statu quo copies retenu, symlink durable differe post-migration)_
- **BPS-9** `fait` — E1 dérive doc-dans-code : index.js:7 décrit 'compileToBPxAST → {ast,backticks,flagStates,libraries}' ; réel = {ast,errors,warnings} (bpxAst.js:180). Le code tranche  _(fait: 2026-07-19 — mesure des champs rendus : ast, errors, warnings, les trois et pas plus. En-tete corrige, avec la raison ecrite sur place : le meme jour, un commentaire perime de lib/controls.json a fait croire a une regression majeure et failli declencher une refonte de l AST. Une doc qui ment coute plus cher qu une doc absente.)_
- **BPS-10** `fait` — E5 garde structurel non branché au gate (pas de script arch ; Node 20+ requis via nvm, défaut dépôt = 18)  _(fait: garde déplacé à la racine + script arch (8ff36f8))_
- **LIB-RAGA** `requalifié/arbitrage-romain` [P3] — Alphabet 'raga' absent d'alphabets.json → ~~repli western~~ **REJET FAIL-LOUD** (alias raga→sargam manquant). Lacune lib distincte de SARG-1  _(2026-07-19 : le CONSTAT tient — `raga` est bien absent de `lib/alphabets.json` (seul `sargam` existe). Mais le SYMPTOME decrit est perime : il n y a PLUS de repli western silencieux, `@alphabet.raga` est desormais REJETE — « alphabet 'raga' introuvable dans le catalogue ». La question devient donc : faut-il AJOUTER l alias raga→sargam, ou le rejet est-il la bonne reponse (raga et sargam ne sont pas synonymes — un raga est un mode, le sargam un systeme de degres) ? C est une question de MODELE, pas une lacune de fichier — a poser avec la musicologie, pas a combler par un alias de commodite.)_
- **MOH-ALIGN** `fait` [P3] — Aligner la fixture canonique BPscript/scenes/mohanam.bps sur la notation saptak (la copie biblio Kanopi est passée en saptak ; l'originale reste en numérique sa6/sa7)  _(fait: mohanam.bps aligné saptak (6874eda, S1 42/0))_
- **TAAR-TOK** `fait` [P1] — Tokeniseur vs convention saptak : 'taar sa' (préfixe d'octave séparé par ESPACE) est découpé en 2 terminaux, 'taar' perdu → registre aigu jamais résolu. Proposer le fix canonique (tokenisation consciente du préfixe d'octave VS séparateur collé pour saptak). Décision langage Romain.  _(en-cours: Romain valide '_' : bpscript change octaves.json separator ' '→'_' pour saptak (+ 4 autres préfixe pour cohérence). taar_sa = 1 token → taar 480 Hz. Puis Kanopi met mohanam à jour (taar sa→taar_sa).)_  _(en-cours: FAIT côté langage (189128b) : separator '_' pour saptak+4 préfixes, prouvé taar_sa→480/mandra_sa→120/madhya défaut, S1 42 OK 0 FAIL, bundle frais, LANGUAGE.md MAJ. Reste : Kanopi met mohanam à jour (taar sa→taar_sa) + vérifie taar audible.)_  _(fait: COMPLET : langage (bpscript 189128b, separator '_') + scène (Kanopi 9a99584, mohanam taar sa→taar_sa) + VÉRIFIÉ écran : gamme sargam complète 240/303.8/360/405 + TAAR à 480 Hz (=2×240, octave au-dessus, registre confortable, sans stridence). Oracle 0 échec.)_
- **ORACLE-DEUX-SOURCES** `ouvert` [P2] — DEUX ORACLES NATIFS DIVERGENTS, laquelle fait foi par grammaire ? Mes 55 snapshots `s3_native.json` et la baseline de bp3-engine mesurent la meme chose et NE COINCIDENT PAS. Mesure du 2026-07-19, un par un sur le contenu (condition posee par bp3-engine, et c est elle qui a evite que je supprime les 55 en croyant a une redondance) : sur les 39 comparables en MIDI, **23 identiques, 16 divergents**. Les ecarts ne sont pas cosmetiques — `ruwet` 2655 jetons contre 126, `koto3` 312 contre 2, `simpletemplates` 198 contre 7, `PP` 73 contre 2, `dhati` 23 contre 66 ; `MyMelody` a le MEME compte mais un minutage entierement different (`mi4@0-750` contre `mi4@-50-185`) ; `harmony` diverge sur `Db4` contre `C#4`. Trois allures probables, NON VERIFIEES : action `produce-all` contre jeu simple, reglages `-se` differents, orthographe enharmonique (zone de convention ambigue que bp3-engine a lui-meme signalee sur 5 grammaires). Ce ne sont vraisemblablement PAS deux verites contradictoires mais deux captures sous conditions differentes — a etablir avec bp3-engine, pas a deviner. **Mes 55 snapshots n ont plus de producteur** (le pipeline S0-S5 l a emporte le 2026-07-19) ; bp3-engine, lui, a `capture.py` versionne et un mode a la demande (`capture.py <grammaire>`). NON URGENT : ne bloque pas le chemin critique. NE PAS supprimer les 55 avant reconciliation.  _(2026-07-19, RESOLU PAR LA MESURE — et la cause n etait dans AUCUNE de mes trois hypotheses. (1) bp3-engine a mesure la cause dominante (commit 1b798f2, baseline-native/README.md) : sa baseline force MaxItemsProduce=1 (une realisation, graine fixe) la ou mes instantanes laissaient le reglage du fichier, souvent 20. En retirant le forcage : ruwet 126 -> 2528 (le mien 2655), PP 2 -> 26 (73), koto3 2 -> 22 (312), simpletemplates 7 -> 16 (198). L ordre de grandeur bascule. Mon hypothese `produce-all` etait FAUSSE, mesuree : produce-all n emet aucun jeton. (2) dhati 23 contre 66 = ma photo PRECEDE son correctif de chargement des objets sonores (sans -so.dhati : 23 ; avec : 66). Verifie chez moi : le `23` est `s3_native.json`, date du 2026-07-16. (3) harmony Db4/C#4 = la zone des cinq conventions ambigues, meme son. **DONC : deux captures sous conditions differentes, jamais deux verites contradictoires** — ce que je supposais sans pouvoir le prouver, et pour quoi j avais refuse de supprimer. La condition « un par un » que bp3-engine m avait posee nous a attrapes TOUS LES DEUX le meme jour : il avait lui aussi generalise depuis une grammaire.  **MAIS EN VERIFIANT, J AI TROUVE PIRE, ET CHEZ MOI.** Le probleme n a jamais ete « mes 55 contre sa baseline ». Mesure du jour sur mes 118 grammaires a instantanes : **75 d entre elles (64 %) ont des instantanes qui se contredisent ENTRE EUX sur le compte de jetons**, avant toute comparaison exterieure. dhati porte a lui seul 276 / 0 / 87 / 23 / 0 / 37 jetons selon le fichier. Huit voies (s0_php, s1_native, s2_orig, s3_silent, s3_timed, s4_silent, s5_bps, s3_native), **20 dates de capture distinctes** etalees de mars a juillet, et **aucun fichier ne declare ses conditions**. Ce n est pas un oracle, c est un empilement de photos anonymes. La question « laquelle fait foi » ne se pose donc pas entre deux sources mais entre HUIT, dont sept n ont plus de producteur. REQUALIFIEE : ce n est plus une reconciliation avec bp3-engine (faite), c est une PURGE a instruire chez moi — ne garder que ce qui declare ses conditions et sait etre reproduit.)_  _(2026-07-19, LE COMPTE AVANT LA PURGE — ET IL CONTREDIT MA PROPRE ANNONCE. J avais ecrit « sept voies sur huit n ont plus de producteur », et j en avais tire qu elles etaient purgeables. Mesure des LECTEURS vivants (hors worktrees d agents) : **CINQ voies sur huit sont lues par du code vivant**, essentiellement chez BPx. `s5_bps` a DIX lecteurs (BPx test/parity/*, src/load/loadGrammar.ts) ; `s3_native` en a neuf, dont un chez moi (`src/transpiler/orderTokens.js`) et un chez bp3-frontend (`src/aux/settings.ts`) ; `s1_native` six, dont un banc de Kairos ; `s2_orig` trois ; `s3_timed` deux. **N avoir plus de PRODUCTEUR n est pas n avoir plus de LECTEUR** — c est la distinction que mon annonce ecrasait, et supprimer sur cette base aurait casse les bancs de parite de BPx en bloc. Restent VRAIMENT orphelines : `s0_php` (107 fichiers), `s4_silent` (101), `s3_silent` (32) — 240 fichiers, 14,1 Mo. MAIS sur ces 240, **146 portent des jetons** et 20 seulement sont vides : elles ATTESTENT un contenu que plus rien ne sait regenerer (le pipeline S0-S5 est supprime). Or on vient de voir avec `silent.al` de vina qu un artefact derive orphelin peut porter une reponse juste que personne n avait lue. **JE NE SUPPRIME DONC RIEN.** Ce que ces fichiers n ont pas, ce n est ni un lecteur ni un contenu : c est une DECLARATION DE CONDITIONS, et supprimer ne la fabrique pas. Le vrai chantier n est pas une purge, c est de rendre les conditions LISIBLES — ou d acter que ces 240 fichiers sont des pieces d archive, nommees comme telles, qu on ne cite plus comme oracle. A trancher, pas a executer.)_
- **E-017** `fait` — E-017 : scene bells derive 18 tokens != 16 vs natif (status.json s2_vs_s3=DIFF) — realignement scene, signale par bp3-frontend au solde F4-CTX remote  _(fait: verifie le 2026-07-19 en sortie de chaine — bells rend 16 jetons contre 16 a la reference, statut ISO au nommage pres (registre aligne d une octave via C4key, Hz identiques). L ecart 18 contre 16 n existe plus.)_
- **BPS-DEMO-TRANSPORTMIDI** `fait` [P5] — Démo public/demos/midi-actors.bps emploie la forme périmée 'transport:midi' (canon obsolète, devrait être '.midi') — repéré par Atlas sur le fil-rouge d'onboarding. Non bloquant, cosmétique doc/démo  _(fait: verifie le 2026-07-19 — la demo porte DEJA le canon : public/demos/midi-actors.bps:7 ecrit 'transport.midi(ch:1)', forme a point. Entree perimee.)_
- **BPS-TUNING-CATALOGUE** `fait` [P2] — USER-FACING : démos tuning-just.bps (@tuning:Cmaj) et tuning-raga.bps (@alphabet.raga) référencent des accordages qui vivent dans lib/tuning.json, HORS des 5 catalogues PITCH_LIB servis → jouaient un reniflage western tempéré en silence (l'inverse de leur titre). Kairos échoue désormais bruyamment. Investiguer : que contient scales.Cmaj (vraie intonation juste ?) ; DÉCIDER périmètre catalogue = servir tuning.json OU corriger les démos. L'oracle e2e REF_TUNING_JUST avait figé les mauvais Hz (tempérés) = à refaire une fois le bon accordage servi  _(fait: verifie sur pieces le 2026-07-19, sur TROIS fronts. (1) Les demos ne portent plus les references hors catalogue : tuning-just.bps:7 ecrit '@tuning.western_just' (et non '@tuning:Cmaj'), tuning-raga.bps:6 ecrit '@alphabet.sargam' (et non '@alphabet.raga'). (2) Elles RESOLVENT correctement, mesure en sortie de chaine : tuning-just rend C4=264.00 E4=330.00 G4=396.00 — intonation JUSTE exacte (264x5/4=330, 264x3/2=396) ; tuning-equal rend C4=261.63 C#4=277.18 — tempere, bien distinct ; tuning-raga rend sa=240.00 re=269.39. Les demos jouent donc ce qu elles annoncent. (3) L oracle REF_TUNING_JUST n existe nulle part dans ce depot (seule occurrence : cette entree de backlog) — s il subsiste, c est cote Kanopi, hors de mon perimetre.)_
- **BPSCRIPT-BPS-TRAD-AUDIT** `ouvert` [P3, A REQUALIFIER] — Audit per-scene des .bps traduites : 7 scenes utilisent (control) inline (suffixe-symbole 765432/12345678/transposition3, groupe tryGraphics/tryMIDIfile) au lieu du flux — potentielles mauvaises traductions du forward-only BP3. 2 oracles ACTIFS (765432, transposition3) : per-scene, comparaison .gr, mesure oracle, PAS de batch (flipper containment vers flux change le byte-id). Prioriser les 'diff' (flux = iso win potentiel).  _(2026-07-19 : le PROTOCOLE est perime — il repose sur la comparaison .gr et le byte-id, disparus avec l encodeur. Le BESOIN survit et se remesure en sortie de chaine. Mesure du jour sur les deux oracles ACTIFS que l entree nomme : 765432 est DIFF au rang 0 (reference `do3@0-166` contre candidat `do4@0-166` — un REGISTRE, pas un containment) et transposition3 est DIFF au rang 0 (reference `E1@-10-1300` contre candidat `E1@1-1300` — un ONSET NEGATIF cote natif, pas un containment non plus). Donc sur les deux cas mesurables, l hypothese de l entree (mauvaise traduction du forward-only par du containment) n est PAS ce que la mesure montre. A requalifier sur ces observations plutot qu a poursuivre sur l hypothese d origine.)_
- **BPS-11** `en-cours` [P2] — DHATI-RNG-RESIDU : dhati A=24/B=32 (mode:random) — A≡B ne converge pas ; caractériser A vs natif avant tout fix (Voie B seed/mode ou inherent ?)  _(2026-07-19 : MESURE contre la baseline v12, en sortie de chaine — reference 66 jetons, candidat 32, premiere divergence au rang 0 : le natif rend `do5@0-1000` la ou je rends `do5_do5do5@0-2000`. Mon candidat produit donc un terminal COMPOSE (objet sonore concatene) la ou le natif rend trois jetons distincts. C est une piste CONCRETE et nouvelle — ni un residu de graine ni un ecart de mode comme le supposait l intitule. A creuser sur cette base plutot que sur l ancienne hypothese RNG.)_  _(en-cours: REOUVERT : dhati N EST PAS native-broken — c etait notre harnais S0 (omet -al alphabet). Avec ./bp3 v3.4.4 + -al.dhati + -gl.D50 (INIT neutralise) = 880 octets, item polymetrique reel (preuve bp3-engine #53). dhati RE-ENTRE validable. Reste : re-mesurer A/B vs ce vrai oracle.)_
- **BPS-12** `fait` [P3] — DHATI-TEMPLATE-ANCHOR-SPACING : dernier ecart byte-id dhati = espacement ancres gabarit '(= N14 )' vs '(=N14)' (encoder TemplateMasterGroup, pre-existant, hors piege fleche)  _(fait: corrige par 5642bc7. Verifie sur pieces le 2026-07-19 : dhati gram#1[4] rend '(=+ S64 ;)' des deux cotes, byte-identique au natif.)_
- **BPS-13** `fait` [P3] — DHATI-ORACLE-NATIF-VERIF : dhati a-t-il UNE invocation native valide (-ho.dhati/INIT) ou S0 est genuinement casse sur bols indiens ? Domaine bp3-engine (owner oracle natif, OFFLINE) -> verifier au retour. Si oracle existe, dhati re-entre validable.  _(fait: REPONDU par la baseline v12 de bp3-engine — dhati y est PRODUCTIBLE, action `single`, modalite MIDI, 66 jetons, avec ses trois auxiliaires declares (`-se.dhati`, `-al.dhati`, `-so.dhati`). L oracle natif existe donc bien et S0 n etait pas casse sur les bols indiens : il lui manquait l alphabet. dhati est validable.)_
- **BPS-14** `ouvert` [P2, A REQUALIFIER] — DHATI-TABLA-SUB-DIVERGENCE : A!=B localise voix tabla (A bols avec silences -ti- ; B bols nus ti ; timings differents), sous-gram SUB 4 V-expansion/5 bols/6 cadences. Triage byte-id .bps vs -gr natif -> si .bps==natif = bug BPx SUB (peut toucher autres tabla) ; sinon transcription. Ne PAS fixer dhati (ungreenable), router le finding.  _(2026-07-19 : le PROTOCOLE cite est perime — le triage byte-id .bps contre -gr supposait l encodeur BP3, supprime. Le triage se refait desormais en sortie de chaine contre la baseline. La QUESTION survit (bug BPx SUB ou transcription ?) et la mesure du jour lui donne une piste : le candidat rend un terminal COMPOSE `do5_do5do5` la ou le natif rend des jetons distincts — ce qui ressemble plus a une transcription qu a un defaut de SUB, mais je ne le tranche pas.)_
- **BPS-15** `abandonné` [P2] — ENCODER-OPERATOR-GLUING : l encoder .bps->BP3 ESPACE les operateurs (+ - ; *) que le natif COLLE aux operandes (bols composes -ti- +tidha ktti- ; templates (=A)(:A)) -> BPx voit N terminaux au lieu d 1 compose -> SUR-PRODUCTION (affecte l A=B reel, pas que byte-id). MEME axe qu anchorPad (5642bc7) + checktemplates (=A)(:A). SYSTEMIQUE : caracteriser la regle de collage depuis la source BP3 (quels operateurs collent, dans quels contextes) puis fix encoder d un coup -> peut greener plusieurs grammaires tabla/template validables. Attention : regle SUBTILE (certains espacent, cf anchorPad SAUF frontiere operateur) -> pas de collage aveugle.  _(abandonné: RETRACTE (Romain avait raison) : caracterise a la source — operateurs +/* : le lecteur de terminal BP3 s arrete dessus quel que soit l espacement (CompileProcs.c:355), preuve -gr.dhadhatite (ecrit +S2F colle ET ++ S2F espace). Espacement = cosmetique byte-id, zero effet sonnant. Seul - ambigu, mais AUCUNE grammaire active n a de - interne (dhati excluded ; dhin/polyphony to_be_tested). BPS-15 fait bouger ZERO grammaire active.)_
- **BPS-16** `ouvert` [P1, A REQUALIFIER] — GRAMMARS-JSON-PHPREF-7 : ajouter le bloc php_ref (+status active) pour les 7 grammaires recuperees par bp3-engine (dhati, 12345678, 765432, Nadaka, trial.mohanam, tryRagas, tryObjects) — elles produisent 0-erreur une fois l alphabet -al passe (deja gere par s0 si php_ref.alphabet present). Spec dhati fournie : alphabet -al.dhati, settings -se.dhati, note_convention french. Modele = dhati2/dhati3. Demander a bp3-engine les specs des 6 autres. -> re-entrent validable.  _(2026-07-19 : le MECANISME cite n existe plus — « deja gere par s0 si php_ref.alphabet present » renvoie a `s0_snapshot.cjs`, supprime avec le pipeline S0-S5. Le BESOIN peut survivre (ces 7 grammaires produisent-elles dans la chaine actuelle ?) mais il se repose sur la mesure Voie B, pas sur un bloc php_ref lu par un outil disparu. A requalifier avant d etre chiffre.)_
- **BPS-17** `fait` [P1] — BP3TOSCENE-ALPHABET-HEADER : bp3ToScene.js:234-235 (/^-[a-z]{2}\./) DROP silencieusement la ref alphabet d en-tete (-ho.abc / -al.X) -> Voie A perd l alphabet -> GetBols ne regroupe pas les LHS multi-car (BPx FIDELE, port CompileGrammar.c:1107-1175). Fix = traduire l en-tete -ho.<name>/-al.<name> en @test_alphabets.<name>, ne PAS dropper. SYSTEMIQUE (meme famille alphabet-perdu que le trou harnais s0). Revele par checkSUB1 (le patch manuel 795e440 masquait le bug). Angle mort de la cert byte-id : elle verifiait les REGLES, pas la declaration d ALPHABET.  _(fait: verifie sur pieces le 2026-07-19 — l en-tete est CAPTUREE (bp3ToScene.js:225 alphabetRefs, :248 collecte, :608 emission) et non plus jetee. Mesure : -gr.checkSUB1 porte '-ho.abc' et la conversion emet '@test_alphabets.abc' en premiere ligne. Corrige de longue date par [603] ; l entree de backlog etait perimee.)_
- **BPS-18** `ouvert` [P3, INCHANGE] — INIT-PLAY-RUNTIME (valide Romain 2026-07-18) : INIT: Play <item> = reglage de demarrage (ex I46 = _script(MIDI program 30)). N affecte PAS la derivation (dhati sort 491o identiques INIT strippe). BP3 l execute via primitives fenetre GUI -> injouable headless (partage oracle+WASM). SOLUTION RATIFIEE : traiter INIT:Play au niveau RUNTIME (preambule de reglage), PAS reparer le chemin GUI du moteur C. Le moteur derive la structure, le runtime execute le reglage de depart. Pas un blocage iso. A porter cote runtime pour la vraie lecture (P3).
- **BPS-19** `ouvert` [P2] — GRAMMARS-JSON-PHPREF-LOT2 : +4 grammaires recuperees par bp3-engine (tri des 13) a passer active+php_ref : Djinns, checkVolMasterSlave, testHO2, tryKeyXpand (retrait -or./ajout -al). ATTENTION livecode2 recuperee MAIS 854 Mo (pathologique comme tryRagas) -> NE PAS ajouter au compte, instruire d abord. Demander specs a bp3-engine.
- **BPS-20** `ouvert` [P3] — NADAKA-RESIDU-4-JETONS : apres exclusion des non-sonnants + resolution P->pa4, il reste 4 jetons d ecart A/B non expliques (566 vs 570). Transcription prouvee fidele (meme texte, P=abrev solfege indien). A instruire, non bloquant (1 grammaire, couche resolution).
- **BPS-21** `ouvert` [P3] — MOHANAM-SHAPESRHYTHM-DECALAGE-TRANSCRIPTION : la fuite de mode masquait un decalage de transcription PRE-EXISTANT sur mohanam et shapes-rhythm (revele par le fix currentMode 5b1f072). mohanam reste A=B 27=27 (n affecte pas le sonnant) ; shapes-rhythm est excluded. Non traite dans 5b1f072, a instruire. Faible priorite.
- **BPS-22** `abandonné` [P2] — S0-WARN-SKIP-EXCLUDED : s0_snapshot.cjs:172 saute SILENCIEUSEMENT les grammaires excluded -> sortie vide trompeuse (gramgene1/2 semblaient muettes, sortent 884/372 une fois actives). FABRIQUE de faux 'ne produit rien' pour tout agent qui mesure avec cet outil. FIX = emettre un WARNING explicite au skip (pas silencieux). Durcissement harnais, meme famille que les trous -al/-so/vieux-format.  _(abandonné: SANS OBJET — s0_snapshot.cjs a ete SUPPRIME le 2026-07-19 avec tout le pipeline S0-S5 (charte de purge). Plus d outil, plus de skip silencieux a durcir.)_
- **BPS-23** `ouvert` [P3] — DIAGNOSTIC-METRIC-LHS-FAILLOUD : dhadhatite_v2 scene.bps compile 0 erreur mais la mesure jette 'compileTemplates: malformed scale 4+4+4+4/4 (expected /N,*N,*N/D)' — l annotation de metrique LHS part dans le chemin scale/template et produit un message TROMPEUR au lieu d un fail-loud clair 'annotation de metrique en partie gauche non supportee'. Fix = fail-loud explicite. (Le trou de langage lui-meme = LANG-METRIC-ANNOTATION-LHS, a trancher avec Romain.)
- **BPS-24** `ouvert` [P1] — CONVERTER-DEGENERATE-VALUES (P1) : convertOldSettings (s0_snapshot.cjs) produit des valeurs DEGENEREES systematiques sur les -se convertis : MaxConsoleTime=1 (au lieu de 1000, COUPE la prod), A4freq=10 (440), C4key=10 (60), DeftVelocity=10 (64), DeftVolume=10 (90), SamplingRate=10 (50). Invisible sur grammaires courtes/bols-only, TRONQUE/FAUSSE les longues/a-notes. Fondation de BPE-7 atteinte -> apres fix, bp3-engine re-convertit les 84 + re-verifie les 8 recuperees. Trouve par bpscript (koto1 7 vs 72).  _(2026-07-19 : l outil PORTEUR du bug (s0_snapshot.cjs) a ete supprime de BPscript avec le pipeline S0-S5. MAIS le BUG lui-meme n est pas clos : il decrit une conversion de -se dont bp3-engine se sert pour sa baseline. Reste ouvert et change de PROPRIETAIRE — a confirmer avec bp3-engine, je ne le clos pas depuis mon poste.)_
- **BPS-25** `abandonné` [P2] — CONVERTER-3-COPIES : le meme bug de carte champ->position existe dans s1_native.cjs, s2_wasm_orig.cjs, s3_native.cjs (3 autres copies de convertOldSettings). Non touchees (code non mesure). A corriger avec la meme verif une fois la carte BP2 obtenue (idealement factoriser en une seule source). Depend de la carte BP2 (bloque BPS-24).  _(abandonné: SANS OBJET cote BPscript — les trois copies vivaient dans s1_native.cjs/s2_wasm_orig.cjs/s3_native.cjs, supprimes le 2026-07-19 avec le pipeline S0-S5.)_
