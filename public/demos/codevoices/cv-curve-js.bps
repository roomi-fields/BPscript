// Voix de code — le calcul dans les backticks, l'ordonnancement dans la grammaire
//
// BPScript ORDONNANCE, le code CALCULE. La voix de code reçoit son moment de la
// grammaire ; ce qu'elle fait de ce moment lui appartient. Ici trois cellules
// appellent le même bout de code à trois instants différents, et c'est la structure
// — pas le code — qui décide quand.

core
eval.js
alphabet.western:audio
tempo:90

-----
S -> Cellule Cellule Cellule

-----

mode:ord

Cellule -> D4 A4 `js: console.log('cellule 1')` (vel:60, wave:sine, attack:6, release:200)
Cellule -> D4 A4 `js: console.log('cellule 2')` (vel:84, wave:sine, attack:6, release:200)
Cellule -> D4 A4 `js: console.log('cellule 3')` (vel:108, wave:sine, attack:4, release:240)
